<?php
/**
 * Included from Minz_Extension::getConfigureView(), so $this is the extension
 * itself — not the view.
 * @var ExtensionUpdaterExtension $this
 */
$ext = $this;
?>
<form method="post" action="<?= _url('extension', 'configure', 'e', urlencode($ext->getName())) ?>">
	<input type="hidden" name="_csrf" value="<?= FreshRSS_Auth::csrfToken() ?>" />
	<div class="form-group">
		<label class="group-name" for="use_official_index"><?= _t('ext.extension_updater.conf.official_index') ?></label>
		<div class="group-controls">
			<input type="checkbox" name="use_official_index" id="use_official_index" value="1"
				<?= $ext->confBool('use_official_index', true) ? 'checked' : '' ?> />
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="official_index_url"><?= _t('ext.extension_updater.conf.official_index_url') ?></label>
		<div class="group-controls">
			<input type="url" name="official_index_url" id="official_index_url" size="70"
				value="<?= htmlspecialchars($ext->confString('official_index_url', EUIndexSource::OFFICIAL_URL), ENT_QUOTES, 'UTF-8') ?>" />
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="custom_indexes"><?= _t('ext.extension_updater.conf.custom_indexes') ?></label>
		<div class="group-controls">
			<textarea name="custom_indexes" id="custom_indexes" rows="4" cols="70"
				placeholder="https://example.org/extensions.json"><?= htmlspecialchars($ext->confString('custom_indexes', ''), ENT_QUOTES, 'UTF-8') ?></textarea>
			<p class="help"><?= _t('ext.extension_updater.conf.custom_indexes_help') ?></p>
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="enable_github"><?= _t('ext.extension_updater.conf.enable_github') ?></label>
		<div class="group-controls">
			<input type="checkbox" name="enable_github" id="enable_github" value="1"
				<?= $ext->confBool('enable_github', true) ? 'checked' : '' ?> />
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="github_token"><?= _t('ext.extension_updater.conf.github_token') ?></label>
		<div class="group-controls">
			<input type="password" name="github_token" id="github_token" size="50" autocomplete="off"
				value="<?= htmlspecialchars($ext->confString('github_token', ''), ENT_QUOTES, 'UTF-8') ?>" />
			<p class="help"><?= _t('ext.extension_updater.conf.github_token_help') ?></p>
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="enable_git"><?= _t('ext.extension_updater.conf.enable_git') ?></label>
		<div class="group-controls">
			<input type="checkbox" name="enable_git" id="enable_git" value="1"
				<?= $ext->confBool('enable_git', true) ? 'checked' : '' ?> />
			<p class="help"><?= _t('ext.extension_updater.conf.enable_git_help') ?></p>
		</div>
	</div>

	<div class="form-group">
		<label class="group-name" for="cache_ttl_hours"><?= _t('ext.extension_updater.conf.cache_ttl') ?></label>
		<div class="group-controls">
			<input type="number" name="cache_ttl_hours" id="cache_ttl_hours" min="0" max="168"
				value="<?= (int)$ext->confInt('cache_ttl_hours', 6) ?>" />
		</div>
	</div>

	<div class="form-group form-actions">
		<div class="control-group">
			<button type="submit" class="btn btn-important"><?= _t('gen.action.submit') ?></button>
		</div>
	</div>
</form>

<p>
	<a href="<?= Minz_Url::display(['c' => 'updater', 'a' => 'index']) ?>">
		<?= _t('ext.extension_updater.conf.open_overview') ?>
	</a>
</p>
